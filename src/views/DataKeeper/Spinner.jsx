import { Spinner } from "@blueprintjs/core";
import React, { Component } from "react";

const SmallSpinner = () => {
  return <Spinner size={20} intent={"success"} />;
};
export default SmallSpinner;
